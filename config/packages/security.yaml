security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User

    # ==========================
    # Firewalls
    # ==========================
    firewalls:
        # désactive la sécurité pour les assets et le profiler
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        main:
            lazy: true
            provider: app_user_provider
            user_checker: App\Security\UserChecker

            # Form login classique
            form_login:
                login_path: app_login           # route vers le login form
                check_path: app_login           # route pour le POST de login
                username_parameter: _username
                password_parameter: _password          
                success_handler: App\Security\LoginSuccessHandler
                enable_csrf: true               # protection CSRF activée
                csrf_parameter: _csrf_token
                csrf_token_id: authenticate

            # Déconnexion
            logout:
                path: app_logout
                target: app_login

            # Se souvenir de l'utilisateur
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800 # 1 semaine

    access_control:
        # Pages publiques
        - { path: ^/login, roles: PUBLIC_ACCESS }
        - { path: ^/register, roles: PUBLIC_ACCESS }
        - { path: ^/logout, roles: PUBLIC_ACCESS }

        # Dashboard réservé aux utilisateurs connectés
        - { path: ^/dashboard, roles: ROLE_USER }

        # Zone admin
        - { path: ^/admin, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
